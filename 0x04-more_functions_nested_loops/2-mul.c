#include "main.h"
/**
 * nul - Multiplie two integers 
 * @x: The first number to multily
 * @y: The second number to multiply with the first number
 * Return: the result of multplication between x parm an y parm
 */
int nul(int x, int y)
{
	return (x * y);
}
